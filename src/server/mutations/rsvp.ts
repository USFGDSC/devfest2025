// RSVP-related mutations

export const createRsvp = async (eventId: string, userId: string) => {
  // Create RSVP logic
  return null;
};

export const cancelRsvp = async (eventId: string, userId: string) => {
  // Cancel RSVP logic
  return null;
};
